package EightAM.asteroids;

import android.content.Context;

//public class SmallAlien extends Alien implements Shooter {

    /**
     * Spawns a new small alien on the screen at a random y position on either
     * the left or the right of the screen
     * @param xTotalPix total x dimensions of the screen
     * @param yTotalPix total y dimensions of the screen
     * @param context context of the game (passed from game model)
     */
    /*
    protected SmallAlien(int xTotalPix, int yTotalPix, Context context) {
        super(xTotalPix, yTotalPix);
        // prepare bitmap
        if (bitmap == null) bitmap = ImageUtils.getVectorBitmap(context, R.drawable.ic_small_alien);
    }*/
//}
